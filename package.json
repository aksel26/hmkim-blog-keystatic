{
  "name": "hmkim-blog-keystatic",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.2.0",
  "scripts": {
    "dev": "turbo dev",
    "dev:blog": "turbo dev --filter=blog",
    "dev:agent": "turbo dev --filter=agent",
    "dev:agent-web": "turbo dev --filter=agent-web",
    "build": "turbo build",
    "lint": "turbo lint",
    "generate-post": "turbo generate-post --filter=agent"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
