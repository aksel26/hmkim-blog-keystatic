# 신규 기능 구현 계획 (v2)

> 작성일: 2026-01-22
> 피드백 반영: 2026-01-22

---

## 변경사항 요약

| 항목 | 기존 계획 | 피드백 반영 |
|------|-----------|-------------|
| 구독 폼 위치 | 블로그 하단/사이드바 | **전용 페이지 `/subscribe`** |
| 이메일 인증 | Double opt-in (인증 필요) | **Single opt-in (즉시 구독)** |
| 개인정보 동의 | 없음 | **체크박스 + 모달 추가** |
| 구독 완료 | 단순 안내 | **canvas-confetti 축하 효과** |
| 구독자 관리 | blog 앱 | **agent-web 앱으로 이동** |
| 이메일 템플릿 | 고정 템플릿 | **관리자가 편집 가능** |
| 이메일 발송 트리거 | 수동/Webhook | **GitHub Actions (PR merge)** |
| 스케줄 톤 설정 | 있음 | **제거** |

---

## 1. apps/blog 구독 기능

### 개요
블로그 방문자가 `/subscribe` 페이지에서 구독 신청 → 즉시 구독 완료 → PR merge 시 자동 이메일 발송

### 기술 스택
- **이메일 발송**: Resend
- **데이터베이스**: Supabase (PostgreSQL)
- **축하 효과**: canvas-confetti

### 필요 패키지
```bash
# apps/blog
pnpm add canvas-confetti
pnpm add -D @types/canvas-confetti
```

### 파일 구조
```
apps/blog/
├── app/
│   ├── subscribe/
│   │   ├── page.tsx              # 구독 신청 페이지
│   │   └── complete/
│   │       └── page.tsx          # 구독 완료 페이지 (confetti)
│   └── api/
│       └── subscribe/route.ts    # POST: 구독 신청 API
├── components/
│   ├── SubscribeForm.tsx         # 구독 폼 컴포넌트
│   ├── PrivacyModal.tsx          # 개인정보 동의 모달
│   └── SubscribeButton.tsx       # 메인 페이지 구독 버튼
└── lib/
    └── supabase.ts               # Supabase 클라이언트
```

### 구현 상세

#### 1.1 구독 신청 페이지 (`/subscribe`)

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│                    📬 뉴스레터 구독                          │
│                                                             │
│         새로운 글이 발행되면 이메일로 알려드립니다              │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 이름                                                │    │
│  │ [홍길동                                        ]    │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 이메일 *                                            │    │
│  │ [example@email.com                            ]    │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                             │
│  [✓] 개인정보 수집 및 이용에 동의합니다 [자세히 보기]         │
│                                                             │
│              [ 동의 및 구독하기 ]                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 1.2 개인정보 동의 모달

```
┌─────────────────────────────────────────────────────────────┐
│  개인정보 수집 및 이용 동의                           [X]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 수집 항목: 이름, 이메일 주소                             │
│                                                             │
│  2. 수집 목적: 뉴스레터 발송                                 │
│                                                             │
│  3. 보유 기간: 구독 해지 시까지                              │
│                                                             │
│  4. 동의 거부 권리: 동의를 거부할 수 있으며,                  │
│     거부 시 뉴스레터 구독이 제한됩니다.                       │
│                                                             │
│                                        [ 확인 ]             │
└─────────────────────────────────────────────────────────────┘
```

#### 1.3 구독 완료 페이지 (`/subscribe/complete`)

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│                    🎉 구독이 완료되었습니다!                  │
│                                                             │
│                      (canvas-confetti)                      │
│                                                             │
│         새로운 글이 발행되면 이메일로 알려드릴게요.            │
│                                                             │
│         문의사항은 kevinxkim2023@gmail.com으로               │
│         연락주세요.                                          │
│                                                             │
│                   [ 블로그로 돌아가기 ]                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### API 명세

#### POST /api/subscribe
```typescript
// Request
{
  name: string;
  email: string;
  privacyAgreed: boolean;  // 필수: true
}

// Response 200
{ success: true }

// Response 400
{ success: false, error: "이미 구독 중인 이메일입니다." }
{ success: false, error: "개인정보 수집에 동의해주세요." }
```

### 구현 단계

| 순서 | 작업 | 파일 |
|------|------|------|
| 1 | Supabase 클라이언트 설정 | `lib/supabase.ts` |
| 2 | 구독 API 구현 | `app/api/subscribe/route.ts` |
| 3 | 개인정보 모달 컴포넌트 | `components/PrivacyModal.tsx` |
| 4 | 구독 폼 컴포넌트 | `components/SubscribeForm.tsx` |
| 5 | 구독 신청 페이지 | `app/subscribe/page.tsx` |
| 6 | 구독 완료 페이지 (confetti) | `app/subscribe/complete/page.tsx` |
| 7 | 메인 페이지 구독 버튼 추가 | `components/SubscribeButton.tsx` |

---

## 2. apps/agent-web 구독 관리 시스템

### 개요
관리자가 구독자 목록을 관리하고, 이메일 템플릿을 작성/편집하는 기능

### Supabase 스키마
```sql
-- subscribers 테이블 (blog와 공유)
CREATE TABLE subscribers (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  email TEXT UNIQUE NOT NULL,
  name TEXT,
  status TEXT DEFAULT 'active' CHECK (status IN ('active', 'unsubscribed')),
  privacy_agreed_at TIMESTAMPTZ NOT NULL,
  subscribed_at TIMESTAMPTZ DEFAULT now(),
  unsubscribed_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

-- 인덱스
CREATE INDEX idx_subscribers_email ON subscribers(email);
CREATE INDEX idx_subscribers_status ON subscribers(status);

-- RLS 활성화
ALTER TABLE subscribers ENABLE ROW LEVEL SECURITY;

-- email_templates 테이블
CREATE TABLE email_templates (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  name TEXT NOT NULL,
  subject TEXT NOT NULL,           -- 이메일 제목 템플릿
  body TEXT NOT NULL,              -- 이메일 본문 템플릿 (HTML)
  is_default BOOLEAN DEFAULT false,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

-- 기본 템플릿 데이터
INSERT INTO email_templates (name, subject, body, is_default) VALUES (
  '새 글 알림',
  '{{blog_name}}에 새 글이 발행되었습니다: {{post_title}}',
  '<h1>{{post_title}}</h1><p>{{post_summary}}</p><a href="{{post_url}}">글 읽기</a>',
  true
);
```

### 파일 구조
```
apps/agent-web/
├── app/
│   ├── subscribers/
│   │   ├── page.tsx              # 구독자 목록 페이지
│   │   └── [id]/
│   │       └── page.tsx          # 구독자 상세/편집 페이지
│   ├── templates/
│   │   ├── page.tsx              # 템플릿 목록 페이지
│   │   ├── new/page.tsx          # 새 템플릿 생성
│   │   └── [id]/
│   │       └── edit/page.tsx     # 템플릿 편집 페이지
│   └── api/
│       ├── subscribers/
│       │   ├── route.ts          # GET: 목록, POST: 생성
│       │   └── [id]/route.ts     # GET, PUT, DELETE
│       └── templates/
│           ├── route.ts          # GET: 목록, POST: 생성
│           └── [id]/route.ts     # GET, PUT, DELETE
├── components/
│   ├── subscribers/
│   │   ├── SubscriberList.tsx    # 구독자 목록
│   │   ├── SubscriberCard.tsx    # 구독자 카드
│   │   └── SubscriberForm.tsx    # 구독자 편집 폼
│   └── templates/
│       ├── TemplateList.tsx      # 템플릿 목록
│       ├── TemplateCard.tsx      # 템플릿 카드
│       ├── TemplateForm.tsx      # 템플릿 편집 폼
│       └── TemplatePreview.tsx   # 템플릿 미리보기
└── lib/
    ├── subscribers/
    │   ├── types.ts              # 타입 정의
    │   └── manager.ts            # CRUD 로직
    └── templates/
        ├── types.ts              # 타입 정의
        └── manager.ts            # CRUD 로직
```

### UI 와이어프레임

#### 구독자 목록 페이지 (`/subscribers`)
```
┌─────────────────────────────────────────────────────────────┐
│ 📧 구독자 관리                              총 150명 (활성)  │
├─────────────────────────────────────────────────────────────┤
│ [검색: 이메일 또는 이름          ] [상태: 전체 ▼]           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ ┌───┬─────────────┬────────────────────┬────────┬────────┐ │
│ │ # │ 이름        │ 이메일              │ 상태   │ 작업   │ │
│ ├───┼─────────────┼────────────────────┼────────┼────────┤ │
│ │ 1 │ 홍길동      │ hong@example.com   │ ✅ 활성│ [편집] │ │
│ │ 2 │ 김철수      │ kim@example.com    │ ✅ 활성│ [편집] │ │
│ │ 3 │ 이영희      │ lee@example.com    │ ⛔ 해지│ [편집] │ │
│ └───┴─────────────┴────────────────────┴────────┴────────┘ │
│                                                             │
│ < 1 2 3 ... 10 >                                           │
└─────────────────────────────────────────────────────────────┘
```

#### 템플릿 관리 페이지 (`/templates`)
```
┌─────────────────────────────────────────────────────────────┐
│ 📝 이메일 템플릿 관리                        [+ 새 템플릿]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 새 글 알림                                    ⭐ 기본   │ │
│ │ 제목: {{blog_name}}에 새 글이 발행되었습니다            │ │
│ │ 수정일: 2026-01-20                                      │ │
│ │ [미리보기] [편집] [기본으로 설정]                        │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 주간 뉴스레터                                          │ │
│ │ 제목: 이번 주 {{blog_name}} 하이라이트                  │ │
│ │ 수정일: 2026-01-15                                      │ │
│ │ [미리보기] [편집] [기본으로 설정] [삭제]                 │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 템플릿 편집 페이지
```
┌─────────────────────────────────────────────────────────────┐
│ 템플릿 편집: 새 글 알림                                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ 템플릿 이름                                                 │
│ [새 글 알림                                           ]    │
│                                                             │
│ 이메일 제목                                                 │
│ [{{blog_name}}에 새 글이 발행되었습니다: {{post_title}}]   │
│                                                             │
│ 사용 가능한 변수:                                           │
│ {{blog_name}}, {{post_title}}, {{post_summary}},           │
│ {{post_url}}, {{subscriber_name}}, {{unsubscribe_url}}     │
│                                                             │
│ 이메일 본문 (HTML)                                          │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ <h1>{{post_title}}</h1>                                 │ │
│ │ <p>안녕하세요, {{subscriber_name}}님!</p>               │ │
│ │ <p>{{post_summary}}</p>                                 │ │
│ │ <a href="{{post_url}}">글 읽기</a>                      │ │
│ │ <hr>                                                    │ │
│ │ <a href="{{unsubscribe_url}}">구독 해지</a>             │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ 미리보기                                                    │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ (렌더링된 HTML 미리보기)                                 │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│                              [취소]  [저장]                 │
└─────────────────────────────────────────────────────────────┘
```

### 구현 단계

| 순서 | 작업 | 파일 |
|------|------|------|
| 1 | Supabase 테이블 생성 | SQL |
| 2 | 타입 정의 | `lib/subscribers/types.ts`, `lib/templates/types.ts` |
| 3 | 구독자 매니저 구현 | `lib/subscribers/manager.ts` |
| 4 | 템플릿 매니저 구현 | `lib/templates/manager.ts` |
| 5 | 구독자 API 구현 | `app/api/subscribers/` |
| 6 | 템플릿 API 구현 | `app/api/templates/` |
| 7 | 구독자 UI 컴포넌트 | `components/subscribers/` |
| 8 | 템플릿 UI 컴포넌트 | `components/templates/` |
| 9 | 구독자 페이지 구현 | `app/subscribers/` |
| 10 | 템플릿 페이지 구현 | `app/templates/` |
| 11 | 사이드바 네비게이션 추가 | - |

---

## 3. GitHub Actions 자동 이메일 발송

### 개요
`main` 브랜치에 PR이 merge되면 자동으로 구독자에게 이메일 발송

### 워크플로우 파일
```yaml
# .github/workflows/newsletter.yml
name: Send Newsletter on PR Merge

on:
  pull_request:
    types: [closed]
    branches: [main]
    paths:
      - 'apps/blog/content/**/*.mdoc'

jobs:
  send-newsletter:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v44
        with:
          files: |
            apps/blog/content/**/*.mdoc

      - name: Extract post info
        id: post-info
        run: |
          # 변경된 mdoc 파일에서 포스트 정보 추출
          for file in ${{ steps.changed-files.outputs.added_files }}; do
            # frontmatter에서 title, summary 추출
            echo "Processing $file"
          done

      - name: Send newsletter
        if: steps.changed-files.outputs.added_files != ''
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          BLOG_URL: ${{ vars.BLOG_URL }}
        run: |
          # agent-web API 호출하여 이메일 발송
          curl -X POST "${{ vars.AGENT_WEB_URL }}/api/newsletter/send" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.NEWSLETTER_API_KEY }}" \
            -d '{
              "postTitle": "${{ steps.post-info.outputs.title }}",
              "postSummary": "${{ steps.post-info.outputs.summary }}",
              "postUrl": "${{ vars.BLOG_URL }}/post/${{ steps.post-info.outputs.slug }}"
            }'
```

### agent-web 뉴스레터 발송 API

```
apps/agent-web/
└── app/api/
    └── newsletter/
        └── send/route.ts    # POST: 뉴스레터 발송
```

#### POST /api/newsletter/send
```typescript
// Headers
{ Authorization: "Bearer ${NEWSLETTER_API_KEY}" }

// Request
{
  postTitle: string;
  postSummary: string;
  postUrl: string;
}

// Response 200
{
  success: true,
  sent: 150,
  failed: 2
}
```

### 구현 단계

| 순서 | 작업 | 파일 |
|------|------|------|
| 1 | 뉴스레터 발송 API 구현 | `app/api/newsletter/send/route.ts` |
| 2 | GitHub Actions 워크플로우 작성 | `.github/workflows/newsletter.yml` |
| 3 | GitHub Secrets 설정 | GitHub Settings |
| 4 | Resend 이메일 발송 로직 | `lib/email/sender.ts` |

---

## 4. 자동 포스팅 스케줄러 (수정)

### 변경사항
- **톤(Tone) 설정 필드 제거**

### 수정된 Supabase 스키마
```sql
-- schedules 테이블 (tone 필드 제거)
CREATE TABLE schedules (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  name TEXT NOT NULL,
  description TEXT,

  -- 토픽 설정
  topic_source TEXT DEFAULT 'manual' CHECK (topic_source IN ('manual', 'rss', 'ai_suggest')),
  topic_list TEXT[],
  topic_index INTEGER DEFAULT 0,
  rss_url TEXT,
  ai_prompt TEXT,

  -- 작업 설정 (tone 제거)
  category TEXT DEFAULT 'tech' CHECK (category IN ('tech', 'life')),
  template TEXT DEFAULT 'default',
  target_reader TEXT,
  keywords TEXT[],
  auto_approve BOOLEAN DEFAULT false,

  -- 스케줄 설정
  cron_expression TEXT NOT NULL,
  timezone TEXT DEFAULT 'Asia/Seoul',
  enabled BOOLEAN DEFAULT true,

  -- 실행 기록
  last_run_at TIMESTAMPTZ,
  next_run_at TIMESTAMPTZ,
  last_job_id UUID REFERENCES jobs(id),
  run_count INTEGER DEFAULT 0,
  error_count INTEGER DEFAULT 0,
  last_error TEXT,

  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
```

### 수정된 스케줄 생성 폼 (톤 제거)
```
┌─────────────────────────────────────────────────────────────┐
│ 새 스케줄 만들기                                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ 기본 정보                                                   │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 이름 *                                                  │ │
│ │ [주간 기술 트렌드                               ]       │ │
│ │                                                         │ │
│ │ 설명                                                    │ │
│ │ [매주 월요일 최신 기술 트렌드 블로그 자동 생성  ]       │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ 토픽 설정                                                   │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 토픽 소스 *                                             │ │
│ │ (●) 수동 목록  ( ) RSS 피드  ( ) AI 생성               │ │
│ │                                                         │ │
│ │ 토픽 목록 (줄바꿈으로 구분)                             │ │
│ │ ┌─────────────────────────────────────────────────────┐ │ │
│ │ │ React 19 새 기능 정리                               │ │ │
│ │ │ Next.js 16 마이그레이션 가이드                      │ │ │
│ │ └─────────────────────────────────────────────────────┘ │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ 작업 설정                                                   │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 카테고리 *              템플릿                          │ │
│ │ [tech        ▼]        [tutorial     ▼]                │ │
│ │                                                         │ │
│ │ 타겟 독자                                               │ │
│ │ [프론트엔드 개발자                              ]       │ │
│ │                                                         │ │
│ │ 키워드 (쉼표로 구분)                                    │ │
│ │ [React, Next.js, TypeScript                     ]       │ │
│ │                                                         │ │
│ │ [ ] 자동 승인 (human review 건너뛰기)                   │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ 스케줄 설정                                                 │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 실행 주기 *                                             │ │
│ │ [매주    ▼] [월요일 ▼] [09:00 ▼]                       │ │
│ │                                                         │ │
│ │ 크론 표현식: 0 9 * * 1                                  │ │
│ │ 다음 실행: 2026-01-27 09:00 (Asia/Seoul)               │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│                              [취소]  [스케줄 생성]          │
└─────────────────────────────────────────────────────────────┘
```

### 수정 필요 파일

| 파일 | 작업 |
|------|------|
| `lib/scheduler/types.ts` | tone 필드 제거 |
| `lib/scheduler/manager.ts` | tone 관련 로직 제거 |
| `components/schedules/ScheduleForm.tsx` | tone 입력 필드 제거 |
| `app/api/schedules/route.ts` | tone 파라미터 제거 |

---

## 구현 우선순위

### Phase 1: 구독 기능 기반
1. Supabase 테이블 생성 (subscribers, email_templates)
2. blog 구독 페이지 구현
3. agent-web 구독자/템플릿 관리 UI

### Phase 2: 이메일 자동화
4. Resend 연동 및 이메일 발송 API
5. GitHub Actions 워크플로우 설정

### Phase 3: 스케줄러 수정
6. 스케줄 테이블 생성 (tone 제외)
7. 스케줄 CRUD API/UI
8. Vercel Cron 설정

---

## 환경변수 총정리

### apps/blog
```env
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJxxx
```

### apps/agent-web
```env
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJxxx
SUPABASE_SERVICE_ROLE_KEY=eyJxxx
RESEND_API_KEY=re_xxxxx
NEWSLETTER_API_KEY=your-secure-api-key
CRON_SECRET=your-secure-cron-secret
```

### GitHub Secrets
```
RESEND_API_KEY
SUPABASE_URL
SUPABASE_SERVICE_ROLE_KEY
NEWSLETTER_API_KEY
```

### GitHub Variables
```
BLOG_URL=https://your-blog.com
AGENT_WEB_URL=https://your-agent-web.com
```

---

## 참고 문서

- [Resend Documentation](https://resend.com/docs)
- [Supabase Database](https://supabase.com/docs/guides/database)
- [canvas-confetti](https://www.npmjs.com/package/canvas-confetti)
- [GitHub Actions](https://docs.github.com/en/actions)
- [Vercel Cron Jobs](https://vercel.com/docs/cron-jobs)
