/**
 * 글쓰기 규칙 (writer·creator 공용)
 *
 * 번역투와 AI가 쓴 티가 나는 표현을 막는다. 여기를 고치면 두 에이전트에 함께 반영된다.
 * 규칙마다 X → O 예시를 붙였다. 예시 없이 "자연스럽게 써라"만 지시하면 모델이 거의 따르지 않는다.
 */

export const KOREAN_STYLE_RULES = `
### 문장 규칙 (반드시 지킬 것)

**1. 번역투를 쓰지 않는다.** 영어 문장을 직역한 표현은 우리말 어순으로 바꾼다.
- "~에 대해 / ~에 관해" → 목적어로 바로 쓴다. (X "캐시에 대해 설명합니다" → O "캐시를 설명합니다")
- "~을 통해 / ~함으로써" → "~해서", "~하면", "~로" (X "옵션을 통해 제어합니다" → O "옵션으로 제어합니다")
- "~에 의해 / ~되어진다" → 능동으로 쓴다. (X "요청은 브라우저에 의해 취소됩니다" → O "브라우저가 요청을 취소합니다")
- "~하는 것이 가능합니다 / ~할 수 있습니다"를 습관처럼 붙이지 않는다. 가능 여부를 말할 때만 쓴다. (X "설정할 수 있습니다"가 연달아 나옴 → O "설정합니다")
- "~의 경우 / ~에 있어서 / ~측면에서" → 뺀다. (X "모바일의 경우" → O "모바일에서는")
- "~를 가지고 있다 / ~중 하나이다" → 서술어로 푼다. (X "세 가지 옵션을 가지고 있습니다" → O "옵션이 세 가지입니다")
- "그것, 이것, 그들" 같은 대명사와 불필요한 복수형 "~들"을 줄인다. 가리키는 말을 다시 쓰거나 생략한다.
- 명사를 길게 잇지 않고 동사로 푼다. (X "요청 취소 처리 수행" → O "요청을 취소합니다")
- 한 문장에는 한 가지 내용만 담는다. 쉼표로 절을 세 개 이상 잇지 말고 문장을 나눈다.

**2. AI가 쓴 티가 나는 표현을 쓰지 않는다.**
- 상투적인 도입과 맺음을 쓰지 않는다: "최근 ~가 주목받고 있습니다", "~에 대해 알아보겠습니다", "본 포스트에서는 ~를 살펴봅니다", "결론적으로", "요약하자면", "도움이 되셨기를 바랍니다", "지금 바로 시작해보세요". 첫 문장부터 구체적인 상황이나 문제로 들어간다.
- 과장하거나 포장하는 말을 뺀다: "완벽 정리", "총정리", "A부터 Z까지", "심층 분석", "획기적", "혁신적", "강력한", "놀라운", "필수적인", "매우 중요한", "다양한", "효과적으로", "원활하게", "손쉽게". 빼도 뜻이 같으면 뺀다.
- 정보가 없는 문장을 지운다. 앞 내용을 되풀이하거나 "이는 매우 중요한 부분입니다"처럼 아무것도 알려주지 않는 문장이 해당한다.
- "서론", "본론", "결론", "마무리"를 소제목으로 쓰지 않는다. 소제목에는 그 절이 다루는 내용을 적는다.
- 소제목을 "A: B" 꼴로 짓지 않는다.
- 습관적인 셋 묶음 나열("빠르고, 안전하고, 효율적인")을 피한다. 실제로 셋일 때만 셋을 쓴다.
- 굵은 글씨를 문단마다 넣지 않는다. 글 전체에서 놓치면 안 되는 용어 몇 개에만 쓴다.
- 불릿은 나열할 항목이 실제로 있을 때만 쓴다. 설명은 문단으로 쓴다.
- 구분선(---)으로 절을 나누지 않는다. 소제목이면 충분하다.
- 우리말 용어마다 괄호로 영어를 병기하지 않는다. 처음 나오는 전문 용어에 한 번만 붙인다.
- 같은 길이, 같은 어미의 문장만 잇지 않는다. 짧은 문장과 긴 문장을 섞는다.

기술 용어와 코드 식별자는 원문 그대로 둔다.
이 규칙은 분량을 줄이라는 뜻이 아니다. 군더더기를 뺀 자리는 구체적인 설명, 예시, 근거로 채운다.
`;

/** writer용. 참고 자료를 받는 단계라 자료를 기준으로 삼는다 */
export const ACCURACY_RULES = `
### 정확성 규칙 (반드시 지킬 것)
- 수치, 버전, 날짜, 제품 사양, API·옵션 이름과 기본값은 참고 자료에 있는 것만 쓴다. 자료에 없으면 지어내지 말고 그 문장을 빼거나 단정하지 않는 표현으로 쓴다.
- 참고 자료끼리 내용이 다르면 공식 문서나 1차 출처 쪽을 따른다.
- 출처 없는 통계와 가상의 인용을 만들지 않는다.
- 코드 예제에는 실제로 존재하는 API만 쓴다. 확신이 없으면 예제를 넣지 않는다.
`;

/** creator용. 참고 자료 없이 초안을 다듬는 단계라 초안의 사실 관계를 기준으로 삼는다 */
export const ACCURACY_RULES_FOR_EDITING = `
### 정확성 규칙 (반드시 지킬 것)
- 초안에 없는 수치, 버전, 날짜, 제품 사양을 새로 넣지 않는다.
- 초안의 사실 관계를 바꾸지 않는다. 피드백이나 검증 지적이 요구할 때만 고친다.
- 고칠 근거가 없는 지적은 단정 표현을 빼는 쪽으로 반영한다. 새 사실을 지어내서 메우지 않는다.
`;

/** 메타데이터(title, summary)용 */
export const TITLE_RULES = `
**중요 - 과장 표현 금지**:
- title과 summary에 "완벽 정리", "총정리", "완벽 가이드", "A to Z", "모든 것", "심층 분석", "한 번에" 같은 표현을 쓰지 않는다.
- 글이 실제로 다루는 내용을 그대로 적는다. (X "React Query 캐시 완벽 정리" → O "React Query에서 refetch가 취소되는 조건")
`;
